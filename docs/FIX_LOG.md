# سجل الإصلاحات — Fix Log

## 2026-06-27 — تبسيط النظام لأرشفة إلكترونية فقط

قرر صاحب المشروع أن النظام هو **أرشيف إلكتروني للقيود** وليس نظام مطابقة محاسبية. الهدف: المحاسب يرفع المرفقات، والمراجع يطلع عليها من السيستم. لا OCR ولا مطابقة آلية.

### التعديلات المُطبَّقة

#### 1. حذف الـ OCR والمطابقة بالكامل
- حذف مجلد `ocr_service/` بالكامل (3 ملفات)
- حذف `OCR_MANAGER` و import الـ `ocr_service` من `app.py`
- حذف دوال: `handle_extract`, `handle_matching_run`, `handle_matching_decision`, `handle_matching_results`, `calculate_match`, `fuzzy_score`, `field_value`
- حذف endpoints: `/api/documents/{id}/extract`, `/api/matching/run`, `/api/matching/{id}/confirm`, `/api/matching/{id}/reject`, `/api/matching/results`
- حذف `matches` من استجابة `/api/entries/{trans_no}`
- حذف فحص Tesseract من `handle_settings`

#### 2. تبسيط الـ Workflow لـ 3 حالات فقط
```
STATUSES = ["بانتظار المرفقات", "تم رفع المرفقات", "مؤرشف"]
WORKFLOW_ACTIONS = {
    "archive": "مؤرشف",
    "unarchive": "تم رفع المرفقات",
}
```
حذف الحالات: "عند المراجع"، "تمت المراجعة"، "يحتاج استكمال"، "تمت الأرشفة".
حذف الإجراءات: send-to-reviewer, return-from-reviewer, approve, reject.

#### 3. تبسيط لوحة المراجع → لوحة الأرشفة
- 4 كروت بدلًا من 5: إجمالي القيود / مؤرشفة / قيد المعالجة / بدون مرفقات
- حذف: "عند المراجع"، "قيود بها مخاطر"، "جاهزة للمراجع"
- قائمة الانتظار تعرض الآن كل القيود غير المؤرشفة (بدلًا من قائمة انتظار المراجع)

#### 4. تبسيط التقارير
- حذف: "تقرير المخاطر" و "الجاهزة للمراجع"
- إضافة: "القيود المؤرشفة" (مع تاريخ الأرشفة والمحاسب)

#### 5. تبسيط شاشة القيد
- حذف قسم "المطابقة" بالكامل
- حذف زر "تشغيل OCR" من كل مرفق
- حذف زر "تشغيل المراجعة والمطابقة"
- حذف 5 أزرار workflow → زر واحد: "أرشفة القيد" (أو "إلغاء الأرشفة" لو مؤرشف)
- حذف حقل "الحالة: ... OCR ..." من بطاقة المرفق

#### 6. تبسيط شاشة الإعدادات
- حذف قسم "مزودات OCR"
- إضافة قسم "معلومات الأرشفة" يوضح: نوع النظام، OCR غير مفعّل، المطابقة غير مفعّلة، محلي 100%

#### 7. حذف عنصر القائمة الجانبية
- حذف: "المراجعة والمطابقة"
- تغيير اسم: "لوحة المراجع" → "لوحة الأرشيف"

#### 8. تنظيف المتطلبات
- حذف من `requirements.txt`: `pytesseract`, `rapidfuzz`, `openai`, `pydantic`
- المتبقي: `openpyxl`, `Pillow`, `pillow-heif`, `PyMuPDF`, `python-dotenv`, `qrcode`, `reportlab`, `requests`

### نتائج الاختبار

- ✅ الخادم يبدأ بنجاح ويستجيب `/health`
- ✅ Dashboard يعمل بـ 4 KPIs صحيحة (archived, pending, with_attachments, missing)
- ✅ Stats تعمل بدون `needs_review` أو `matched`
- ✅ Settings تعرض "OCR غير مفعّل" و"محلي 100%"
- ✅ Reports: 7 تقارير (بدون المخاطر والجاهزة للمراجع، مع المؤرشفة)
- ✅ Entry detail: لا يوجد قسم مطابقة، لا يوجد زر OCR
- ✅ Workflow: archive و unarchive يعملان
- ✅ Endpoints المحذوفة ترجع 404
- ✅ UI smoke test: 0 console errors

### ملاحظات للبيئة الإنتاجية

- قاعدة البيانات القديمة ممكن تحتوي قيود بحالة "عند المراجع" أو "تمت المراجعة" — دي هتظهر في الجدول لكنها مش في `STATUSES` الجديد. التحديث لـ "مؤرشف" بيتعمل يدويًا عبر زر الأرشفة في شاشة القيد.
- جدول `matching_results` و `extracted_fields` لسه موجودين في قاعدة البيانات (للـ backward compatibility) لكن مش بيستخدموا.
- مجلد `tessdata` لو موجود مش هيتقرأ — النظام مش بيستدعي Tesseract خالص.
